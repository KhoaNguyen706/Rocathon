# Base URL where the FastAPI backend is reachable from the browser.
# - For local dev: http://localhost:8000
# - For docker-compose: http://localhost:8000 (backend is port-forwarded)
NEXT_PUBLIC_API_URL=http://localhost:8000
